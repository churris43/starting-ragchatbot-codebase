You will be adding changes to the repo and commiting them. It won't push

Task: add and commit changes with a descriptive message. The commit message must be 150 characters maximum. This must not push changes to the repository.

If there is nothing to be commited, return "Nothing to commit"
